# Dev / CI dependencies — enough to run the test suite, linter, and type checker on any machine.
# On the Pi, also install requirements.txt for the hardware libraries
# (RPi.GPIO, spidev), which only build on a Pi.
Pillow~=11.0
requests~=2.32
pytest>=8.0,<10.0
pytest-cov>=5.0,<7.0
ruff>=0.6,<1.0
mypy>=1.10,<2.0
types-requests>=2.32,<3.0
