# Real config contains API key — never commit it
airquality.conf

__pycache__/
*.pyc
*.pyo
.DS_Store
*.swp

# Local virtualenv created by deploy.sh
.venv/

# Legacy cache file (state now lives under $XDG_STATE_HOME/airquality/)
.last_pm25
